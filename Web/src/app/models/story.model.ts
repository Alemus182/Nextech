export class Story {
    id: number;
    url: string;
    title: string;

    constructor(
      id: number,
      url: string,
      title: string
    ){
      this.id = id;
      this.url = url;
      this.title = title;
    }
  }
  